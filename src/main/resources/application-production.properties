# A sample production environment using PostgreSQL database

spring.datasource.url = jdbc:postgresql://localhost/fjorum
spring.datasource.username = fjorum
spring.datasource.password = fjorum
spring.datasource.driverClassName = org.postgresql.Driver

# Specify the DBMS
spring.jpa.database = POSTGRESQL

# Show or not log for each sql query
spring.jpa.show-sql = false

logging.level.org.hibernate.sql = DEBUG

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.generate-ddl = false
#spring.jpa.hibernate.ddl-auto = create

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

#spring.jpa.properties.hibernate.hbm2ddl.import_files = import.sql
#spring.datasource.data = /import.sql


